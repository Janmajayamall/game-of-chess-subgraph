type ExampleEntity @entity {
	id: ID!
	count: BigInt!
	_owner: Bytes! # address
	_operator: Bytes! # address
}

type Game @entity {
	id: ID! # gameId
	boardMapString: String!
	state: Int!
	side: Int!
	winner: Int!
	enpassantSq: BigInt!
	moveCount: Int!
	bkC: Boolean!
	bqC: Boolean!
	wkC: Boolean!
	wqC: Boolean!
}

type Market @entity {
	id: ID! # moveValue
	moveValue: BigInt!
	game: Game!
	moveCount: Int!

	sourceSq: Int!
	targetSq: Int!
	moveBySq: Int!
	sourcePiece: Int!
	targetPiece: Int!
	promotedToPiece: Int!
	moveFlag: Int!

	chosenMove: Boolean!

	tradeVolume: BigDecimal!
}
